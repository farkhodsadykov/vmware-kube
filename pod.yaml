kind: Pod
apiVersion: v1
metadata:
  name: example
spec:
  volumes:
    - name: task-pv-storage
      persistentVolumeClaim:
       claimName: mysql
  containers:
    - name: example
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
        - mountPath: "/usr/share/nginx/html"
          name: task-pv-storage
